<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:clickable="true">

    <ImageView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/thumbnail"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_imageview_border"
        android:scaleType="fitXY" />

    <ImageView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/thumbnail_domain_icon"
        android:layout_width="@dimen/domain_icon_size"
        android:layout_height="@dimen/domain_icon_size"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="2dp"
        android:layout_marginBottom="2dp"
        android:alpha=".5"
        android:visibility="gone" />

    <ImageView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/thumbnail_filetype_icon"
        android:layout_width="@dimen/filetype_icon_size"
        android:layout_height="@dimen/filetype_icon_size"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="3dp"
        android:layout_marginEnd="0dp"
        android:alpha=".5"
        android:visibility="gone" />
</FrameLayout>